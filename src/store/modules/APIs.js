// set and change apis for different pages
const state = {
    post: [''],
};

const getters = {

};

const actions = {

};

const mutations = {
    CHANGE_POST: (state, api) =>{state.post = api},

};

export default {
    state,
    getters,
    actions,
    mutations
};